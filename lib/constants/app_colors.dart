import 'package:flutter/material.dart';

/// Centralized color palette for the app, based on Bumble's brand colors.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFFFC629); // Bumble yellow
  static const Color onPrimary = Colors.black;     // text/icons on yellow

  static const Color background = Colors.white;
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;
}